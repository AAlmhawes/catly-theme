{# ===== Mobile Bottom Navigation ===== #}
<nav class="mobile-nav" role="navigation" aria-label="{{ trans('navigation.home') }}">
  <a href="{{ url('home') }}" class="mobile-nav__item {{ page.is_home ? 'mobile-nav__item--active' : '' }}">
    <svg viewBox="0 0 24 24" fill="currentColor" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round">
      <path d="M3 12 12 3l9 9v9a1 1 0 0 1-1 1h-5v-7H10v7H5a1 1 0 0 1-1-1Z"/>
    </svg>
    <span>{{ trans('navigation.home') }}</span>
  </a>

  <a href="{{ url('products') }}" class="mobile-nav__item {{ page.is_shop ? 'mobile-nav__item--active' : '' }}">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <rect x="3" y="3" width="7" height="7" rx="1"/>
      <rect x="14" y="3" width="7" height="7" rx="1"/>
      <rect x="3" y="14" width="7" height="7" rx="1"/>
      <rect x="14" y="14" width="7" height="7" rx="1"/>
    </svg>
    <span>{{ trans('navigation.shop') }}</span>
  </a>

  <a href="{{ url('wishlist') }}" class="mobile-nav__item {{ page.is_wishlist ? 'mobile-nav__item--active' : '' }}">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
      <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78Z"/>
    </svg>
    <span>{{ trans('common.wishlist') }}</span>
  </a>

  <a href="{{ url('account') }}" class="mobile-nav__item {{ page.is_account ? 'mobile-nav__item--active' : '' }}">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
      <circle cx="12" cy="8" r="4"/><path d="M4 21a8 8 0 0 1 16 0"/>
    </svg>
    <span>{{ trans('common.account') }}</span>
  </a>
</nav>
