{# ===== Trust Badges Strip ===== #}
<section class="trust-badges" aria-label="{{ trans('store_features.label') }}">
  <div class="container">
    <div class="trust-badges__grid">

      <div class="trust-badge">
        <div class="trust-badge__icon" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12H3l9-9 9 9h-2"/><path d="M5 12v7a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-7"/>
            <path d="M9 21v-6a2 2 0 0 1 2-2h2a2 2 0 0 1 2 2v6"/>
          </svg>
        </div>
        <div class="trust-badge__text">
          <strong>{{ trans('store_features.free_shipping_title') }}</strong>
          <span>{{ trans('store_features.free_shipping_description') }}</span>
        </div>
      </div>

      <div class="trust-badge">
        <div class="trust-badge__icon" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            <path d="m9 12 2 2 4-4"/>
          </svg>
        </div>
        <div class="trust-badge__text">
          <strong>{{ trans('store_features.vet_approved_title') }}</strong>
          <span>{{ trans('store_features.vet_approved_description') }}</span>
        </div>
      </div>

      <div class="trust-badge">
        <div class="trust-badge__icon" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8"/><path d="M21 3v5h-5"/>
            <path d="M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16"/><path d="M8 16H3v5"/>
          </svg>
        </div>
        <div class="trust-badge__text">
          <strong>{{ trans('store_features.easy_returns_title') }}</strong>
          <span>{{ trans('store_features.easy_returns_description') }}</span>
        </div>
      </div>

      <div class="trust-badge">
        <div class="trust-badge__icon" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="5" width="20" height="14" rx="2"/>
            <path d="M2 10h20"/>
          </svg>
        </div>
        <div class="trust-badge__text">
          <strong>{{ trans('store_features.secure_payment_title') }}</strong>
          <span>{{ trans('store_features.secure_payment_description') }}</span>
        </div>
      </div>

    </div>
  </div>
</section>
