{# ===== Custom Component: Subscription Banner ===== #}
<section class="subscription-banner" style="background-color: var(--color-secondary); border-radius: 20px; padding: 32px 24px; margin: 32px 0; text-align: center; color: var(--color-text);" aria-labelledby="subscribe-title">
  <h2 id="subscribe-title" style="font-size: 24px; font-weight: 700; margin: 0 0 8px;">
    {{ trans('home.subscribe_save') }}
  </h2>
  <p style="margin: 0 0 20px; font-size: 14px; max-width: 480px; margin-inline: auto;">
    {{ trans('home.subscribe_description') }}
  </p>
  <a href="{{ url('subscriptions') }}" class="btn btn--primary">
    {{ trans('home.shop_now') }}
  </a>
</section>
